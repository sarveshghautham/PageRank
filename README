PageRank Calculation using Hadoop's MapReduce framework on wikipedia data

This project contains four tasks:

Task 1:

Parse a XML file by MapReduce. Fetch the <title> and <text> tags. The <text> tag contains wikilinks. 
The wikilinks are matched with a regular expression and they are removed. Form an adjacency graph with
the page titles and wiki-links.

Task 2:

Counting the number of page titles.

Task 3/4:

Some changes to file write and iteration 1 added.

iteration1 through iteration 8 added

//TODO

Sorting outputs by page rank in descending order, verify output.
